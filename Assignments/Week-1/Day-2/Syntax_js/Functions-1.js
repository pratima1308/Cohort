// Write a function sum that finds the sum of two numbers.
// Side quest - Try passing in a string instead of a number and see what happens?

function sum(a, b) {
  let totalSum = a + b;
  return totalSum;
}
let msg = sum(3, " Pratima");
let msg2 = sum(3, 4);
console.log(msg);
console.log(msg2);
